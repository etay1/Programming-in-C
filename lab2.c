#include <stdio.h>
 
int main () {
printf("Enter two digits");
scanf("%i", n1);
scanf("%i", n2);
co(&n1, &n2);
  return 0;
}
void co (int *x, int *y) {
  
  while (x>0 && y>0) {
  r1 = x%10
  r2 = y%10
  }
   return;
}
